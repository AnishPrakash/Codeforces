#include <stdio.h>
 
int main() {
    int t;
    if (scanf("%d", &t) != 1) return 0;
    
    while (t--) {
        char s[7];
        scanf("%s", s);
        
        if (s[0] + s[1] + s[2] == s[3] + s[4] + s[5]) {
            printf("YES
");
        } else {
            printf("NO
");
        }
    }
    
    return 0;
}